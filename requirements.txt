# Curio Remote Support System - Server Requirements
# Flask web framework and extensions
Flask==2.3.3
Flask-CORS==4.0.0

# Database
sqlite3  # Built-in with Python

# Web server for production
gunicorn==21.2.0

# System monitoring and utilities
psutil==5.9.5

# HTTP requests
requests==2.31.0

# JSON handling (built-in)
# datetime (built-in)
# threading (built-in)
# subprocess (built-in)
# socket (built-in)
# os (built-in)
# sys (built-in)
# time (built-in)
# logging (built-in)

# Security
cryptography==41.0.4

# Network utilities
netifaces==0.11.0

# Configuration management
configparser  # Built-in with Python

# Additional utilities
paramiko==3.3.1  # For SSH connections
Jinja2==3.1.2    # Template engine (Flask dependency)
Werkzeug==2.3.7  # WSGI utilities (Flask dependency)
MarkupSafe==2.1.3  # Safe string handling (Flask dependency)
itsdangerous==2.1.2  # Secure cookie signing (Flask dependency)
click==8.1.7      # Command line interface (Flask dependency)
blinker==1.6.2    # Signal support (Flask dependency) 